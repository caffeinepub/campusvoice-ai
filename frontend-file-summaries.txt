{
  "frontend/src/lib/geminiService.ts": "Gemini AI service using direct REST API fetch calls (no SDK dependency) for complaint text analysis, image analysis, chatbot responses, and predictive analytics. Exports typed functions and interfaces for all AI features.",
  "frontend/src/App.tsx": "Root app shell with 'admin-departments' added to the View type union and DepartmentManagementPage imported and rendered when view === 'admin-departments' (admin-only guard included).",
  "frontend/src/pages/DashboardPage.tsx": "Role-aware dashboard accepting onNavigate prop; shows greeting, stats grid (total/pending/resolved/high-priority), quick action buttons per role (student/hod/staff/admin), and recent complaints preview using useGetMyComplaints and useGetAllComplaints.",
  "frontend/src/pages/SubmitComplaintPage.tsx": "Complaint submission form with AI-powered auto-categorization, live department dropdown from backend with `max-h-72 overflow-y-auto` on SelectContent for scrollability across all 37 departments, category/priority selection, media uploads, anonymous mode toggle, and success confirmation screen.",
  "frontend/src/pages/TrackComplaintsPage.tsx": "Student complaint tracking page with search/filter controls, complaint cards, CSV export, and a detail dialog showing media attachments, AI predictions, and feedback form for resolved complaints.",
  "frontend/src/pages/ChatbotPage.tsx": "Simple page wrapper for the AI chatbot feature, directing users to the floating chat widget.",
  "frontend/src/pages/ComplaintsListPage.tsx": "Complaints list for admin/hod/staff using useUpdateComplaintStatus with correct { id, status } mutation params; supports search/filter, inline status update dropdown, detail dialog, and CSV export with role-based department filtering.",
  "frontend/src/pages/AnalyticsPage.tsx": "Analytics dashboard using runPredictiveAnalysis (correct export name) for AI analysis; shows status/priority/monthly trend charts filtered by department for staff/hod roles, with JSON export and AI analysis button.",
  "frontend/src/pages/UserManagementPage.tsx": "Admin user management page explaining ICP-based user authentication and on-chain profile storage.",
  "frontend/src/pages/EmergencyLogsPage.tsx": "Emergency logs page for admin; displays all recorded alerts with acknowledge/clear actions, accessing extended fields (studentName, location, department, timestamp) via type casting to avoid TypeScript errors.",
  "frontend/tailwind.config.js": "Tailwind CSS configuration with campus teal/navy design system, custom fonts (Inter/Outfit), and all color tokens updated to include `/ <alpha-value>` interpolation throughout — enabling opacity modifiers like `/80`, `/50`, `/70` on every custom color token.",
  "frontend/src/index.css": "Global styles with OKLCH color system, campus design tokens, component classes for status badges, priority indicators, sidebar navigation, and page animations. The `.glass-card` class now uses plain CSS properties instead of `@apply` with opacity modifiers to avoid Tailwind v3 limitations with custom CSS variable colors.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend interactions including user profile, complaints CRUD, admin checks, and new department hooks (useListDepartments, useGetDepartment, useCreateDepartment, useUpdateDepartment, useDeleteDepartment) with proper cache invalidation.",
  "frontend/src/components/DeleteAccountDialog.tsx": "AlertDialog confirmation component for permanent account deletion; now only clears user profile-specific localStorage keys (`campusvoice_profile`, `userProfile`) on deletion, preserving complaint metadata, emergencies, and notifications so submitted complaints remain visible to HOD and Admin after the account is removed.",
  "frontend/src/components/AppHeader.tsx": "Sticky app header with logo, emergency alert button (students only), notification bell (HOD/admin/staff), and user profile dropdown; the `handleLogout` now clears the React Query cache before calling `clear()` to prevent race conditions, and the Delete Account option opens the `DeleteAccountDialog` component.",
  "frontend/src/contexts/AppContext.tsx": "App-wide context managing authentication state, campus role (including staff/admin), user profile, notifications, and emergency alerts; derives role from decoded backend profile name using userProfileHelpers.",
  "frontend/src/components/AppSidebar.tsx": "Role-aware sidebar with a new \"Departments\" nav item (Building2 icon) added to the admin navigation section, wired to the 'admin-departments' view.",
  "frontend/src/pages/LandingPage.tsx": "Rebuilt landing page with four role cards (Student, HOD, Staff, Admin) each navigating to their dedicated login flow; includes features section and footer with caffeine.ai attribution.",
  "frontend/src/components/StudentLoginFlow.tsx": "New dedicated Student login component with Internet Identity authentication; stores `campusvoice_pending_role=student` in localStorage before triggering login so the ProfileSetupModal can pre-select the student role.",
  "frontend/src/components/HODLoginFlow.tsx": "New dedicated HOD login component with Internet Identity authentication; stores `campusvoice_pending_role=hod` in localStorage before triggering login so the ProfileSetupModal can pre-select the HOD role.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal with name, email, role, and department fields; the department SelectContent now has `max-h-72 overflow-y-auto` to ensure all 37 backend departments are scrollable and accessible without truncation.",
  "frontend/src/components/AdminLoginFlow.tsx": "New Admin login flow component with Internet Identity authentication; stores 'admin' as pendingRole in localStorage before login, displays admin-specific benefits (system monitoring, user management, emergency alerts), and uses a red/orange gradient theme.",
  "frontend/src/components/StaffLoginFlow.tsx": "New Staff login flow component with Internet Identity authentication; stores 'staff' as pendingRole in localStorage before login, displays staff-specific benefits (department complaints, status updates, analytics), and uses a purple/indigo gradient theme.",
  "frontend/src/lib/userProfileHelpers.ts": "Utility functions for encoding/decoding pipe-delimited user profile strings; extended to support an optional departmentId field encoded as `|deptId:ID` segment with backward-compatible decoding and a new getDepartmentId helper.",
  "frontend/src/components/EmergencyAlertOverlay.tsx": "Emergency alert overlay for hod/admin/staff roles; accesses extended fields (studentName, location, department, timestamp) via type casting to avoid TypeScript errors on the EmergencyAlert type.",
  "frontend/src/pages/DepartmentManagementPage.tsx": "New admin-only page listing all departments in a table with stats cards (total, with HOD, without HOD), edit/delete action buttons per row, and a \"Add Department\" button that opens the DepartmentFormDialog.",
  "frontend/src/components/DepartmentFormDialog.tsx": "Dialog component for creating and editing departments with name and description fields; supports both create (generates BigInt ID from timestamp) and edit modes, using useCreateDepartment and useUpdateDepartment mutations.",
  "frontend/src/components/DeleteDepartmentDialog.tsx": "AlertDialog confirmation component for deleting a department; shows the department name in the confirmation message and uses useDeleteDepartment mutation with toast feedback.",
  "frontend/src/constants/complaintCategories.ts": "Complaint categories and groups unchanged; the deprecated DEPARTMENTS array is updated to match the exact department names from the backend migration (including correct punctuation like `M.Sc – Microbiology` and `M.A., - English`)."
}